-- Plugins that had `config = true` (lazy.nvim shorthand for `setup({})`)
-- in the previous lazy.nvim spec.
require("guess-indent").setup()
require("marks").setup()

-- nvim-bqf and vim-surround need no setup call.
